# CSG-KP v0.6 — self-contained first-principles manuscript (EN + DE)
Parameter-free relation from the photon conformal anomaly:
`|Omega_K|/Omega_Lambda = a_gamma/(8 pi^2) = 31/(1440 pi^2) ~ 2.181e-3`.
- `en/` English, `de/` German: manuscript (PDF+LaTeX), computing pipeline, cover letter, README.
Each opens with a lay prologue, derives every step from first principles, and isolates the single
physical postulate (A5/P5, Omega_Lambda = 4 a_gamma). Run: `cd en && python3 csg_kp_falsification.py`
(numpy). Exit 0 = all proved checks pass.
